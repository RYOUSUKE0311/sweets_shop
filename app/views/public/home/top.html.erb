<h1 class="mt-2">sweets_shop</h1>
<p>徳島県のお気に入りのスイーツ店を投稿できます。</p>
<div class="border-bottom"></div>

<h2 class="my-4">スイーツショップの新着投稿</h2>
<% if current_user %>
  <div class="row">
    <div class="col-md-4">
      <%= render "public/posts/form", post: @post %>
    </div>
    <div class="col-md-8">
      <%= render "public/posts/search_form", url: root_url %>
      <%= render "public/posts/posts", posts: @posts %>
    </div>
  </div>
<% else %>
  <%= render "public/posts/posts", posts: @posts %>
<% end %>